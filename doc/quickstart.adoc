= Quick Start Guide
:toc: left
:toclevels: 3
:source-highlighter: coderay

This guide will get you up and running with Sandbag in minutes.

== Prerequisites

* Java 8 or later
* https://leiningen.org[Leiningen] build tool
* https://www.datomic.com[Datomic] transactor (dev or pro)

== Installation

Clone the repository and install dependencies:

[source,bash]
----
git clone <repository-url>
cd sandbag
lein deps
----

== Starting the Datomic Transactor

Sandbag requires a running Datomic transactor. Start it in a separate terminal:

[source,bash]
----
# For Datomic Dev
cd /path/to/datomic
bin/transactor config/dev-transactor.properties
----

The default configuration expects Datomic at `datomic:dev://localhost:4334/sandbag`.

== Running the Application

=== Option 1: Direct Run

[source,bash]
----
lein run
----

The server starts on http://localhost:8080.

=== Option 2: REPL (Recommended for Development)

[source,bash]
----
lein repl
----

Once in the REPL:

[source,clojure]
----
;; Start the system
(go)

;; The system is now running:
;; - HTTP server on port 8080
;; - nREPL server on port 28888

;; Stop the system
(stop)

;; Restart after code changes
(stop)
(go)
----

== Verifying the Installation

=== Check System Status

[source,bash]
----
curl http://localhost:8080/api/status
----

Expected response:
[source,json]
----
{
  "time": "2024-01-15T10:30:00.000Z",
  "clojure": {"major": 1, "minor": 12, "incremental": 4}
}
----

=== List All Classes

[source,bash]
----
curl http://localhost:8080/api/store/classes
----

Expected response:
[source,json]
----
{
  "count": 42,
  "classes": ["db.type/bigdec", "db.type/bigint", "db.type/boolean", ...]
}
----

=== Get Class Details

[source,bash]
----
curl http://localhost:8080/api/store/classes/dt/Resource
----

Expected response:
[source,json]
----
{
  "class": "dt/Resource",
  "description": {...},
  "abstract?": false,
  "slots": ["db/doc", "db/ident", "dt/label", "dt/namespace", "dt/type"],
  "subclasses": ["dt/Class", "dt/List", "dt/Literal", "dt/Property", "dt/Ref", ...]
}
----

== Using the REPL

Connect to the nREPL server (port 28888) from your editor, or use the command line:

[source,bash]
----
lein repl :connect 28888
----

=== Querying the Metamodel

[source,clojure]
----
(require '[sandbag.db.datatype :as dt])

;; List all classes
(dt/all-classes)
;; => (:dt/Class :dt/Property :dt/Resource :model/User ...)

;; Get class hierarchy
(dt/ancestors-of :model/User)
;; => (:dt/Ref :dt/Resource)

(dt/subclasses-of :dt/Ref)
;; => (:dt/Any :dt/Fn :model/Twit :model/User)

;; Query slots
(dt/slots-of :model/User)
;; => #{:db/doc :db/ident :dt/label :dt/namespace :dt/type :user/login :user/secret :user/uuid}

;; Check type relationships
(dt/subclass-of? :dt/Resource :model/User)
;; => true
----

=== Creating Typed Entities

[source,clojure]
----
(require '[sandbag.db.datatype :as dt])

;; Create a new User instance
(def alice (dt/make :model/User {:user/login "alice"
                                  :user/secret "hashed-password"}))

;; Verify the type
(:dt/type alice)
;; => :model/User

;; Validate the entity
(dt/valid? alice)
;; => true
----

== Running Tests

[source,bash]
----
# Run all tests
lein test

# Run a specific test namespace
lein test sandbag.api-store-test
----

== Next Steps

* Read the <<architecture.adoc#,Architecture Guide>> to understand the system design
* Explore the <<meta.adoc#,Metamodel Reference>> for the complete type hierarchy
* Check the <<api.adoc#,REST API Reference>> for all available endpoints
* Review the <<development.adoc#,Development Guide>> for extending the system

== Troubleshooting

=== Cannot connect to Datomic

Ensure the Datomic transactor is running:
[source,bash]
----
# Check if transactor is listening
nc -zv localhost 4334
----

=== Port already in use

Change the HTTP port in `config/config.edn`:
[source,clojure]
----
{:http-port 8081}
----

=== Schema not loading

Check that all required schema files are listed in `config/config.edn`:
[source,clojure]
----
{:required-schema [:meta :literal :ref :fn :any :user :twit]}
----

